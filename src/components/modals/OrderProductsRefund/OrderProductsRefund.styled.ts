import styled from 'styled-components'

import { Modal } from 'ui'

export const OrderProductsRefundModal = styled(Modal)``

export const Form = styled.form`
  display: flex;
  flex-direction: column;
  gap: 20px;
`
