export interface ProductBrand {
  id: number
  name: string
  code: string
  icon: string
  slug: string
}

export interface ProductCategory {
  id: number
  title: string
}

export interface PaymentMethod {
  id: number
  title: string
}
